Usage

Backup all buckets:

```bash
./s3-policy-check.sh backup all
```

Compare all buckets after the upgrade:

```bash
./s3-policy-check.sh compare all
```

Single bucket:

```bash
./s3-policy-check.sh backup obi-test-1
./s3-policy-check.sh compare obi-test-1
```
